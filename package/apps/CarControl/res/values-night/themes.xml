<resources>
    <!-- Base night theme, using system native theme to avoid Material dependency -->
    <style name="Theme.CarControl" parent="@android:style/Theme.Black">
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:windowBackground">@android:color/black</item>
        <!-- 这里只能使用系统支持的属性，不能写 colorPrimary、colorSecondary 等 Material 属性 -->
    </style>
</resources>
